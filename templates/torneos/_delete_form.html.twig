<form method="post" action="{{ path('torneos_delete', {'id': torneo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ torneo.id) }}">
    <button class="btn">Eliminar</button>
</form>
