{% extends 'base.html.twig' %}

{% block title %}Torneos{% endblock %}

{% block body %}
    <h1>Torneos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ torneo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ torneo.nombre }}</td>
            </tr>
            <tr>
                <th>Deporte</th>
                <td>{{ torneo.deporte }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ torneo.fecha ? torneo.fecha|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Hora</th>
                <td>{{ torneo.hora ? torneo.hora|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('torneos_index') }}">back to list</a>

    <a href="{{ path('torneos_edit', {'id': torneo.id}) }}">edit</a>

    {{ include('torneos/_delete_form.html.twig') }}
{% endblock %}
